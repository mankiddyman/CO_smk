# Run provenance for the reported species

Frozen 2026-09-21.

- **6303_B_hap1 (Cuscuta)** ran in the since-deleted `CO_snkmk` checkout and has
  no Snakemake metadata in this repo. These copied logs are its ONLY record.
- **Spondias_hap1** -- `recorded_params.txt` is a snapshot of `.snakemake/metadata`
  (via `workflow/scripts/provenance.py`), which is not version-controlled.

The pinned blocks in `config/config.yaml` cite these files.
